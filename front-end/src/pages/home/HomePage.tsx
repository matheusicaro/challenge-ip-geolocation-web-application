import React from 'react';

import IPGeolocation from '../../components/IPGeolocation';

const Home: React.FC = (props) => {
  return (
    <main>
      <IPGeolocation />
    </main>
  );
};

export default Home;
